import SwiftUI

struct LegalDisclaimerView: View {
    var body: some View {
        List {
            Section {
                Text("legal.disclaimer.body")
            }
        }
        .navigationTitle("settings.legal")
    }
}

